#' @details \code{mCov_trans} is a matrix containing the covariates data of
#' the time-invariant covariates that affect the transaction process.
#' Each column represents a different covariate. For every column a gamma parameter
#' needs to added to \code{<%=name_params_cov_trans%>} at the respective position.
#'
#' @details \code{mCov_life} is a matrix containing the covariates data of
#' the time-invariant covariates that affect the lifetime process.
#' Each column represents a different covariate. For every column a gamma parameter
#' needs to added to \code{<%=name_params_cov_life%>} at the respective position.
