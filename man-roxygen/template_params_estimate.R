#' @param clv.data The data object on which the model is fitted.
#' @param start.params.model Named start parameters containing the optimization start parameters for the model without covariates.
#' @param use.cor Whether the correlation between the transaction and lifetime process should be estimated.
#' @param start.param.cor Start parameter for the optimization of the correlation.
#' @param optimx.args Additional control arguments to use during optimization. Forwarded to the optimx function of package optimx.

