#' @description Inherits from \code{clv.time.date} and therefore stores all timepoints
#' with instances of class \code{Date}. Hence, no temporal resolution finer than 1 day is
#' possible with this time unit.
