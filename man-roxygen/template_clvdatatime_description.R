#' @description Inherits from \code{clv.time.datetime} and therefore stores all timepoints
#' with instances of class \code{POSIXct}. Hence, no temporal resolution finer than 1 second is
#' possible with this time unit.
