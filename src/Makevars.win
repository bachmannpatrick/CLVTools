
# needed for gamma function in Armadillo
CXX_STD = CXX11

#PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
#PKG_LIBS = $(SHLIB_OPENMP_CFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)


PKG_CPPFLAGS=-I$(LIB_GSL)/include -I../inst/include
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "RcppGSL:::LdFlags()") -L$(LIB_GSL)/lib/x64 -lgsl -lgslcblas 
