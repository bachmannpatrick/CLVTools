# setMethod(f = "clv.controlflow.estimate.put.optimx", signature = signature(obj="clv.fitted.static.cov"),
#           definition = function(obj, res.optimx){
#
#   # clv.fitted store
#   obj <- callNextMethod()
#
#
#   return(obj)
# })
